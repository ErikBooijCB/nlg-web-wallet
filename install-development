#!/bin/bash

docker-compose up -d && \
    docker exec gulden-wallet-mysql 'mysql -u root -ptesttest < /tmp/pre-migration.sql'

docker exec gulden-wallet-php ./var/www/html/vendor/bin/composer install
